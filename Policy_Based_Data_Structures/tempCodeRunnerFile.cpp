ll arr[n] = {0};
        // loop(i,0,n)
        //     cin >> arr[i];
        // pbds st;
        // ll inversion_count=0;
        // loop(i,0,n){
        //     inversion_count += (st.size() - st.order_of_key(arr[i]));
        //     st.insert(arr[i]);
        // }
        // cout << inversion_count << endl;